using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class Raycaster : MonoBehaviour
{
    // [MenuItem("MyMenu/Do Something")]
    // static void DoSomething()
    // {
    //     var go = GameObject.Find("MFPController");
    //     var playerPosition = go.transform.position;
    //     var targetPosition = new Vector3(-1,0, 0.7f);
    //     var direction = (targetPosition - playerPosition).normalized;
    //     var playerLook = go.transform.forward;
    //     var dot = Vector3.Dot(direction, playerLook );
    //     float cos = dot;
    //     float angle = Mathf.Acos(cos) * Mathf.Rad2Deg;
    //     Debug.Log(angle);
    // }
}